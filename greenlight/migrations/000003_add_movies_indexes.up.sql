CREATE INDEX IF NOT EXISTS watches_brand_idx ON watches USING GIN (to_tsvector('simple', brand));
CREATE INDEX IF NOT EXISTS watches_material_idx ON watches USING GIN (to_tsvector('simple', material));